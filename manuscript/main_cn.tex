\documentclass[11pt]{article}
\usepackage[UTF8]{ctex}
\usepackage{geometry}
\usepackage{amsmath}
\usepackage{booktabs}
\usepackage{array}
\usepackage{hyperref}
\geometry{margin=1in}

\title{DevGuard：小鼠胚胎扰动细胞的阶段特异发育正常性判定框架}
\author{DevGuard working draft}
\date{2026-05-05}

\begin{document}
\maketitle

\section*{拟定英文题目}

\textbf{DevGuard: stage-specific developmental normality boundaries for perturbed mouse embryonic cells}

\section*{一句话主张}

DevGuard 学习小鼠每个发育时间点和谱系的正常范围，并用 conformal calibration 判断扰动细胞是仍然正常、发育延迟、发育提前、命运偏航，还是成为正常图谱外异常状态。

\section*{路线边界}

新版 DevGuard 是独立的小鼠发育扰动正常性判定项目。它不使用 RDEG pipeline、RDEG-derived node graph、OT transition、GRN dynamics 或 matrix-writeback outputs。旧版 DevVCell response-recovery 原型已经归档到：

\begin{verbatim}
Old/legacy_devvcell_response_recovery_20260505/
\end{verbatim}

当前 quick/stress fixture 结果只用于软件验证。真实论文主结论必须来自独立公开小鼠数据，并经过 sample-level split、heldout control FPR、organoid heterogeneity control 和 perturbation/control 对比。

\section*{方法概述}

对 control mouse embryo 或 gastruloid time-course，DevGuard 按时间点 \(t\) 和谱系 \(l\) 建立正常参考集：

\[
N(t,l)=\{x_i: x_i \text{ is a control cell at time } t \text{ and lineage } l\}.
\]

每个正常参考集被划分为 training、calibration 和 heldout test。真实数据优先使用 embryo/organoid/sample-level split，只有在 sample 数不足时才退化到 cell-level fallback，并在 quality table 中标记。

\subsection*{Embedding}

MVP 使用可审计的浅层 embedding：

\begin{enumerate}
  \item library-size normalization；
  \item \texttt{log1p}；
  \item high-variance gene selection；
  \item \texttt{TruncatedSVD} latent embedding。
\end{enumerate}

\subsection*{Nonconformity score}

当前实现两类 score。

\paragraph{KNN distance}
\[
s_{\mathrm{knn}}(x;t,l)=\frac{1}{k}\sum_{j\in KNN_k(x,N_{\mathrm{train}}(t,l))}\lVert z_x-z_j\rVert_2.
\]

\paragraph{Mahalanobis distance}
\[
s_{\mathrm{mah}}(x;t,l)=(z_x-\mu_{t,l})^\top \Sigma_{t,l}^{-1}(z_x-\mu_{t,l}).
\]

\subsection*{Conformal p-value}

对 query cell \(x\)，相对于 stage-lineage reference \(N(t,l)\) 的 conformal p-value 为：

\[
p(x;t,l)=
\frac{1+\#\{s_i^{\mathrm{cal}}\geq s(x;t,l)\}}
{1+n_{\mathrm{cal}}}.
\]

若 \(p(x;t,l)\geq \alpha\)，则认为 \(x\) 仍在该 stage-lineage 正常范围内。当前 MVP 默认 \(\alpha=0.05\)。

\section*{扰动细胞分类}

DevGuard 对每个 perturbed cell 同时计算：

\begin{enumerate}
  \item 当前时间、同谱系：\texttt{p\_current\_same}；
  \item 早期时间、同谱系：\texttt{p\_early\_same}；
  \item 晚期时间、同谱系：\texttt{p\_late\_same}；
  \item 其他谱系：\texttt{p\_other\_lineage}；
  \item 任意正常参考：\texttt{p\_any\_normal}。
\end{enumerate}

分类优先级为：

\[
\texttt{within\_stage\_normal}
>
\texttt{developmental\_delay}
>
\texttt{developmental\_acceleration}
>
\texttt{fate\_deviation}
>
\texttt{abnormal\_off\_normal}.
\]

为避免过度解释边界细胞，当前代码同时输出：

\begin{verbatim}
assigned_class_by_priority
assigned_class_by_max_pvalue
pvalue_margin
ambiguous_flag
n_candidate_classes_passing_alpha
\end{verbatim}

\section*{Developmental Tolerance Index}

对每个 time point、lineage 和 perturbation，定义：

\[
\mathrm{DTI}
=R_{\mathrm{normal}}
-R_{\mathrm{delay}}
-R_{\mathrm{accel}}
-R_{\mathrm{deviation}}
-R_{\mathrm{abnormal}}.
\]

当前实现已加入 bootstrap confidence interval。若有多个 \texttt{sample\_id}，则优先 sample-level bootstrap；若只有一个 sample，则退化为 cell-level bootstrap，并在输出中记录 \texttt{bootstrap\_level}。

\section*{当前实跑结果}

\subsection*{Quick fixture}

Quick fixture 用于验证代码路径和五类分类逻辑。

\begin{center}
\begin{tabular}{lr}
\toprule
项目 & 数值 \\
\midrule
control cells & 1,350 \\
perturbed cells & 120 \\
genes & 90 \\
reference groups & 9 \\
score-method records & 18 \\
mean heldout FPR & 0.0019 \\
max heldout FPR & 0.0333 \\
\bottomrule
\end{tabular}
\end{center}

五类 perturbed cells 各 24 个，分别被分到 \texttt{within\_stage\_normal}、\texttt{developmental\_delay}、\texttt{developmental\_acceleration}、\texttt{fate\_deviation} 和 \texttt{abnormal\_off\_normal}。

\subsection*{Stress fixture}

Stress fixture 在 quick fixture 之外加入 Poisson/gamma count noise、batch shift、donor shift、cell-to-cell variation、dropout、ambiguous intermediate cells 和 label noise。

\begin{center}
\begin{tabular}{lr}
\toprule
项目 & 数值 \\
\midrule
mean heldout FPR & 0.0351 \\
max heldout FPR & 0.2162 \\
within-stage normal cells & 259 \\
fate-deviation cells & 19 \\
developmental-delay cells & 9 \\
developmental-acceleration cells & 1 \\
\bottomrule
\end{tabular}
\end{center}

该结果说明干净 synthetic fixture 会低估 boundary failure risk，因此正式论文必须使用真实 sample-level split。

\subsection*{GSE212050 strict sample-level control reference}

GSE212050 已下载 Seurat RDS，并在完整元数据中筛选能形成严格 sample-level train/cal/test 的 stage-lineage 组。筛选条件为每组至少 200 cells、至少 8 个 sample units，每组最多均衡抽取 1,500 cells，并禁止 cell-level fallback。

\begin{center}
\begin{tabular}{lr}
\toprule
项目 & 数值 \\
\midrule
cells & 13,285 \\
genes & 30,406 \\
reference groups & 9 \\
score-method records & 18 \\
split strategy & sample-level only \\
cell fallback groups & 0 \\
low-heldout records & 0 / 18 \\
mean heldout FPR & 0.0487 \\
max heldout FPR & 0.0723 \\
\bottomrule
\end{tabular}
\end{center}

严格 sample-level rerun 的平均 heldout FPR 接近 \(\alpha=0.05\)，最大 FPR 从早期随机 downsample 的 0.3333 降到 0.0723。所有 reference groups 均使用独立 sample units 进行 train/calibration/test split，且 \texttt{low\_heldout\_flag=False}。这组结果可作为当前真实 control calibration 的主结果，但正式论文仍应在完整数据或更大采样上复核。

\subsection*{MouseGastrulationData WT/Tal1 chimera smoke test}

Bioconductor \texttt{MouseGastrulationData} 已安装，并已导出 embryo atlas sample 1、WT chimera sample 1 和 Tal1 chimera sample 1。WT chimera sample 1 用于 control reference，Tal1 chimera sample 1 用于真实 perturbation classification smoke test。

\begin{center}
\begin{tabular}{lr}
\toprule
WT chimera reference 项目 & 数值 \\
\midrule
reference groups & 12 \\
score-method records & 24 \\
mean heldout FPR & 0.0294 \\
max heldout FPR & 0.1389 \\
split strategy & cell fallback \\
\bottomrule
\end{tabular}
\end{center}

Tal1 classification smoke test：

\begin{center}
\begin{tabular}{lrr}
\toprule
class & cells & fraction \\
\midrule
fate\_deviation & 9,325 & 0.6322 \\
developmental\_delay & 4,862 & 0.3296 \\
abnormal\_off\_normal & 564 & 0.0382 \\
\bottomrule
\end{tabular}
\end{center}

Ambiguity output：

\begin{center}
\begin{tabular}{lrr}
\toprule
ambiguous flag & cells & fraction \\
\midrule
False & 11,721 & 0.7946 \\
True & 3,030 & 0.2054 \\
\bottomrule
\end{tabular}
\end{center}

这组数值不能作为 Tal1 生物学结论，因为目前只导出一个 WT chimera 样本，无法形成独立 embryo-level split。

\subsection*{GSE123187 spatial/tomo preview}

GSE123187 RAW tar 已下载，并新增 tar parser 生成 4-file preview H5AD。

\begin{center}
\begin{tabular}{lr}
\toprule
项目 & 数值 \\
\midrule
cells & 1,536 \\
genes & 24,086 \\
samples & 4 \\
time point & 5dAA \\
mode & single gastruloid \\
\bottomrule
\end{tabular}
\end{center}

当前 preview 缺少 cell type/lineage 注释，因此只能作为 spatial/tomo 数据入口验证，不能直接用于 fate deviation validation。

\section*{当前文件与验证}

核心代码：

\begin{verbatim}
src/devguard/
scripts/devguard/
config/devguard/
docs/DEVGUARD_RESULTS_REPORT_CN.md
\end{verbatim}

验证命令：

\begin{verbatim}
python -m pytest
\end{verbatim}

当前结果：

\begin{verbatim}
12 passed
\end{verbatim}

\section*{下一步}

\begin{enumerate}
  \item 导出完整 MouseGastrulationData embryo atlas、WT chimera、Tal1 chimera 和 T chimera；
  \item 对真实数据强制 embryo/organoid-level split，并过滤低 sample-unit group；
  \item 用完整 GSE212050 做 leave-one-organoid-out false-positive calibration；
  \item 为 GSE123187 补充 spatial/tomo axis 和 lineage mapping；
  \item 所有 perturbation 结论必须与 heldout control FPR 和 organoid heterogeneity FPR 对比后再进入正式 manuscript。
\end{enumerate}

\end{document}
