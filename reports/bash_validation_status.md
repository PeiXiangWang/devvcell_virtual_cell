# Bash Validation Status

`bash --version` was attempted on this Windows workspace and returned a WSL unavailable message. The Unix `bash reproducibility/run_all.sh --quick-fixture` validation was therefore not run. The Windows PowerShell quick fixture validation passed in this environment.
